export function userRequestsClearBoard() {
    
const flagsCounter = document.querySelector("#flags-counter");
const minesCounter = document.querySelectorAll("#mines-counter");

console.log("flagsCounter: " + flagsCounter);
console.log("minesCounter: " + minesCounter);

    
}